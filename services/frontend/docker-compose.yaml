version: "3.9"

services:
   react:
         container_name: react
         restart: on-failure
         build: .
         volumes:
              - .:/app/src
         ports:
              - "3000:3000"
         command: >
              sh -c "npm run dev"